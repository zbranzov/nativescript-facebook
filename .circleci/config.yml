version: 2
jobs:
  build-and-test:
    macos:
      xcode: "9.2.0"
    steps:
      - checkout
      # - run:
      #     name: pre-start simulator
      #     command: xcrun instruments -w "iPhone 6 (10.3.1) [" || true
      #     background: true
      - run:
          name: ruby muby
          command: |
            brew install ruby
            brew link --overwrite ruby
      - run:
          name: prepare
          command: |
            sudo gem install cocoapods
            npm install -g nativescript
            tns usage-reporting disable
            tns error-reporting disable
       #     ruby -v
      #      #sudo gem install bundler
       #     #bundle update
        #    #bundle install
         #   sudo gem install cocoapods
         #   sudo gem install xcodeproj
         #   pod setup
         #   echo ">>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>"
         #   npm install -g nativescript
         #   tns usage-reporting disable
         #   tns error-reporting disable
         # cache_directories:
          #  - "Pods/"
      - run:
          name: build
          shell: /bin/sh
          command: |
            cd demo
            npm run build.plugin
            tns build ios | echo "PASS"
      - run:
          name: build2
          shell: /bin/sh
          command: |
            cd demo
            npm run build.plugin
            tns build ios | echo "PASS"
      - run:
          name: build pass
          command: |
            cd demo
            npm run build.plugin
            tns build ios
      - store_artifacts:
          path: /Users/distiller/.npm/_logs
      
      

workflows:
  version: 2
  build-and-test:
    jobs:
      - build-and-test
